# Disabled workflows

These workflows are kept for reference but are **not active**. GitHub Actions only runs files under `.github/workflows/`.

Production deploys use **Deploy to VPS** (`.github/workflows/deploy.yaml`).

To re-enable a workflow, move it back into `.github/workflows/`.
