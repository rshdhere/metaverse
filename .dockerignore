# Single .dockerignore at repo root. Dockerfiles control what gets copied (COPY).
# Exclude only what is never needed by any image.

# Dependencies and caches (reinstalled or not used in image)
node_modules
.pnp
.pnp.js
.turbo
bun.lockb

# Version control and IDE
.git
.cursor
.vscode
.husky
.github

# Build outputs (rebuilt in image or not used)
.next
out
build
dist
*.tsbuildinfo

# Env and secrets (inject at runtime)
.env
.env.*
!.env.sample
!.env.example

# Testing and debug
coverage
*.log
*.pem

# Docker assets (avoid copying into context)
docker

# Misc
.DS_Store
*.md
