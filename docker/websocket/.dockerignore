# Used when build context is repo root (docker build -f docker/websocket/Dockerfile .).
# Dependencies and caches
node_modules
.pnp
.pnp.js
.turbo
bun.lockb

# Version control and IDE
.git
.cursor
.vscode
.husky
.github

# Build outputs
.next
out
build
dist
*.tsbuildinfo

# Env and secrets (inject at runtime)
.env
.env.*
!.env.sample
!.env.example

# Testing and debug
coverage
*.log
*.pem

# Other apps (websocket image only needs apps/world)
apps/client
apps/server

# Other packages (Go app uses env only; no packages/ needed)
packages

# Docker assets (avoid copying into context)
docker

# Misc
.DS_Store
*.md
